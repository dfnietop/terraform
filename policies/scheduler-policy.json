{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Action": [
                "states:StartExecution"
            ],
            "Resource": [
                "arn:aws:states:us-east-1:${owner_ciencuadras}:stateMachine:${proyecto}-${dominio}-${ambiente}-*"
            ]
        }
    ]
}